# Guess-The-Number-Java
A simple Number Guessing Game developed in Java using Object-Oriented Programming (OOP). The program generates a random number, and the player keeps guessing until the correct number is found. It also counts the total number of attempts.
